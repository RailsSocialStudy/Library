<%= form_for(@book_user) do |f| %>
  <% if @book_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_user.errors.count, "error") %> prohibited this book_user from being saved:</h2>

      <ul>
      <% @book_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :book_id %><br />
    <%= f.text_field :book_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
